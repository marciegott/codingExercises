// 1. Crie um objeto chamado objetoVazio que não tenha nenhuma propriedade.
const objetoVazio = {};

/* 
2. Crie um objeto chamado pessoa com as propriedades:
nome: "Seu Nome" (string)
idade: 30 (number)
*/

const pessoa = {
    nome: 'Marcie',
    idade: 30
};

console.log(pessoa);

console.log('oi');